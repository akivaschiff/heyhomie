anthropic>=0.40.0
python-dotenv>=1.0.0
pytz>=2024.1
requests>=2.31.0

# Voice channel — Deepgram REST (STT/TTS) + Porcupine wake word + mic capture
pvporcupine>=4.0.0
pvrecorder>=1.2.*

# Shared store — Google Drive (system of record)
google-api-python-client>=2.120.0
google-auth>=2.28.0

# Telegram channel
python-telegram-bot>=21.0

# Tracing
langfuse>=3.0.0

# Tests
pytest>=8.0.0
